# DEPRECATED - superseded by deploy.yml (purge job)
# This file is kept as a tombstone to prevent accidental recreation.
#
# This workflow previously ran on every push to main and called the
# Cloudflare purge API with a secret name (CF_API_TOKEN) that was never
# configured, so it FAILED every push with a red X. The surgical,
# secret-guarded purge now lives in .github/workflows/deploy.yml (job 2)
# and skips cleanly when its secrets are absent.
#
# DO NOT re-enable this workflow.
#
# To manually trigger a cache purge + status check:
#   GitHub -> Actions -> "Deploy -> Purge -> Status" -> Run workflow
